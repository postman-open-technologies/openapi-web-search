const URL = 'https://index.commoncrawl.org/';
const SCHEDULE = '0 0 * * 0';
const INDEX_FILES_BATCH_SIZE = 10;
const DELAY = 1000; // 1 second delay

module.exports = {URL,SCHEDULE,INDEX_FILES_BATCH_SIZE,DELAY};
